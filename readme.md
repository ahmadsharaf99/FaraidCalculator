#  An Online Islamic Inheritance Calculator that computes the shares of the prescribed heirs.

##  Starting the Server
### run 'npm install' to install all the dependencies
### run 'npm start' to start the server.